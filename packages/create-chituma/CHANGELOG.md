# create-chituma

## 0.1.0

### Minor Changes

- 7f6712f: Add `create-chituma` — the initializer behind `npm create chituma@latest`, which
  scaffolds a new Chituma project (zero runtime dependencies).

{
  "name": "@redtuma/spec",
  "version": "0.0.0",
  "private": true,
  "description": "Behavioral conformance suite for the Redtuma public API. Independently authored (clean-room); see CLEANROOM.md.",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "@redtuma/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ai": "^4.3.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0",
    "zod": "^3.24.0"
  }
}
